# DDK 에이전트 시스템 가이드

> Devom Development Kit (DDK) AI 에이전트 시스템의 구조와 동작 원리

**작성일**: 2025-11-11
**버전**: 1.1.0 (DDK Frontend Edition)
**대상**: React/Next.js 프론트엔드 개발

---

## 📚 목차

1. [DDK란 무엇인가?](#-ddk란-무엇인가)
2. [전체 아키텍처](#-전체-아키텍처)
3. [핵심 구성 요소](#-핵심-구성-요소)
4. [에이전트 시스템 동작 원리](#-에이전트-시스템-동작-원리)
5. [Self-Contained 프롬프트 철학](#-self-contained-프롬프트-철학)
6. [실제 워크플로우](#-실제-워크플로우)
7. [병렬 실행 최적화](#-병렬-실행-최적화)
8. [실전 예제](#-실전-예제)

---

## 🎯 DDK란 무엇인가?

**DDK (Devom Development Kit)**는 Claude Code와 함께 체계적으로 개발하기 위한 AI 기반 개발 프레임워크입니다.

### 핵심 개념

#### 1. Epic-Driven Development
큰 기능(Epic)을 작은 작업(Task)으로 분해하여 관리합니다.

```
Epic: 사용자 프로필 페이지 추가
  ├─ Task 1: UserProfile 컴포넌트 생성
  ├─ Task 2: UserAvatar 컴포넌트 생성
  ├─ Task 3: useUserData 훅 생성
  ├─ Task 4: 테스트 작성
  └─ Task 5: Storybook 스토리 작성
```

#### 2. AI 에이전트 시스템
각 에이전트가 특정 역할을 담당합니다.

| 에이전트 | 역할 | 담당 업무 |
|---------|------|-----------|
| `component-writer` | React 컴포넌트 작성 | .tsx 파일 생성, Props 타입 정의 |
| `hook-writer` | 커스텀 훅 작성 | useXXX 훅 구현, 비동기 상태 관리 |
| `storybook-writer` | Storybook 작성 | .stories.tsx 파일, 문서화 |
| `code-reviewer` | 코드 리뷰 | 품질 검증, 규칙 준수 확인 |

#### 3. 체계적인 문서화
- **코딩 규칙**: `.devom/docs/coding-rules.md` (937줄)
- **컴포넌트 패턴**: `.devom/docs/component-patterns.md` (1,142줄)
- **테스트 패턴**: `.devom/docs/testing-patterns.md` (913줄)

#### 4. Self-Contained 프롬프트
모든 정보를 프롬프트에 포함하여 에이전트가 독립적으로 작동합니다.

---

## 🏗️ 전체 아키텍처

### 시스템 구조도

```
┌─────────────────────────────────────────────────────────────┐
│                    개발자 (You)                               │
│  "TODO 리스트 만들어줘"                                       │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│             Claude (Scrum Master)                            │
│  역할: Epic 분석, Task 분해, 에이전트 호출, 결과 조합         │
└─────────────────────────────────────────────────────────────┘
                          ↓
        ┌─────────────────┴─────────────────┐
        ↓                                    ↓
┌────────────────┐                  ┌────────────────┐
│ component-writer│                  │  hook-writer   │
│ 에이전트         │                  │  에이전트       │
└────────────────┘                  └────────────────┘
        ↓                                    ↓
   TodoList.tsx                         useTodos.ts
   TodoItem.tsx                         생성 완료
   생성 완료
        ↓                                    ↓
┌─────────────────────────────────────────────────────────────┐
│              code-reviewer 에이전트                          │
│  역할: 모든 코드 리뷰 후 피드백 제공                          │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│             Claude (Scrum Master)                            │
│  "✅ 완료! 다음 파일들이 생성되었습니다..."                    │
└─────────────────────────────────────────────────────────────┘
```

### 디렉토리 구조

```
/Users/dabom-choi/StudySource/devom/

├── .claude/
│   └── agents/                        ← 🤖 에이전트 정의
│       ├── component-writer.md        ← React 컴포넌트 작성
│       ├── hook-writer.md             ← 커스텀 훅 작성
│       ├── storybook-writer.md        ← Storybook 작성
│       └── code-reviewer.md           ← 코드 리뷰
│
├── .devom/
│   ├── docs/                          ← 📚 개발 규칙 문서
│   │   ├── coding-rules.md            ← 코딩 표준
│   │   ├── component-patterns.md      ← 컴포넌트 패턴
│   │   ├── testing-patterns.md        ← 테스트 패턴
│   │   └── agent-system-guide.md      ← 이 문서
│   │
│   ├── scripts/                       ← 🛠️ CLI 도구
│   │   ├── epic-manager.sh            ← Epic 관리
│   │   └── task-manager.sh            ← Task 관리
│   │
│   ├── templates/                     ← 📝 템플릿
│   │   └── task-prompts/              ← Task 프롬프트 템플릿
│   │
│   └── epics/                         ← 📋 작업 디렉토리
│       └── EPIC-{ulid}/               ← 각 Epic 폴더
│           ├── EPIC.md
│           └── TASK-*.md
│
├── CLAUDE.md                          ← 📖 DDK 메인 가이드
├── apps/                              ← 애플리케이션
└── packages/                          ← 공유 패키지
```

---

## 🔧 핵심 구성 요소

### 1. 에이전트 정의 파일 (.claude/agents/)

각 에이전트는 **독립적인 전문가**로 동작합니다.

#### 파일 구조

```
.claude/agents/
├── component-writer.md    → React 컴포넌트 전문가
├── hook-writer.md         → 커스텀 훅 전문가
├── storybook-writer.md    → Storybook 문서화 전문가
└── code-reviewer.md       → 코드 품질 검증 전문가
```

#### 에이전트 정의 파일 템플릿

```markdown
# 에이전트명

## Role (역할)
무엇을 하는 에이전트인가?

## Capabilities (할 수 있는 것)
- ✅ 기능 1
- ✅ 기능 2
- ✅ 기능 3

## Limitations (할 수 없는 것)
- ❌ 제약 1
- ❌ 제약 2

## Input Format (입력 형식)
어떤 형식으로 요청해야 하는가?

## Coding Standards (코딩 표준)
어떤 코드를 작성해야 하는가?

## Workflow (작업 흐름)
어떤 순서로 작업하는가?

## Output Format (출력 형식)
어떻게 결과를 보고하는가?

## Best Practices (모범 사례)
주의해야 할 점은?
```

#### component-writer.md 예시

```markdown
# Component Writer Agent

## Role
프론트엔드 컴포넌트를 설계하고 구현하는 전문 에이전트

## Capabilities
- ✅ React 함수형 컴포넌트 구현
- ✅ TypeScript 타입 정의
- ✅ Props 인터페이스 설계
- ✅ 접근성(a11y) 속성 추가

## Limitations
- ❌ 메모리 파일 읽기 불가 (Self-Contained)
- ❌ 코드 리뷰 수행 불가 (code-reviewer 전담)
- ❌ 스토리북 작성 불가 (storybook-writer 전담)

## Input Format
파일 경로: packages/ui/src/Button/Button.tsx

구현할 코드:
[완전한 TypeScript 컴포넌트 코드]

코딩 규칙:
- 함수형 컴포넌트만 사용
- Props는 interface로 정의
- JSDoc 필수
```

### 2. 개발 문서 (.devom/docs/)

에이전트들이 참고하는 **코딩 바이블**입니다.

#### coding-rules.md 구조

```markdown
# Frontend Coding Rules

## 1. TypeScript 규칙
- ❌ `any` 사용 금지
- ✅ 명시적 반환 타입 정의
- ✅ 제네릭 활용

## 2. 컴포넌트 규칙
- 함수형 컴포넌트만 사용
- Props는 interface로 정의
- 컴포넌트당 최대 200줄
- JSDoc 필수

## 3. 네이밍 컨벤션
- 컴포넌트: PascalCase (UserProfile)
- 함수/변수: camelCase (getUserData)
- 상수: UPPER_SNAKE_CASE (API_BASE_URL)

## 4. 성능 최적화
- useCallback으로 이벤트 핸들러 감싸기
- useMemo로 비싼 계산 최적화
- React.memo 적절히 사용

## 5. 접근성 (a11y)
- 시맨틱 HTML 사용
- ARIA 속성 추가
- 키보드 접근성 보장
```

### 3. Epic & Task 관리 (.devom/epics/)

작업을 체계적으로 관리합니다.

#### 디렉토리 구조

```
.devom/epics/
├── README.md
└── EPIC-m1n2o3p4/           ← Epic 폴더
    ├── EPIC.md              ← Epic 정의
    ├── TASK-001.md          ← Task 1
    ├── TASK-002.md          ← Task 2
    └── TASK-003.md          ← Task 3
```

#### EPIC.md 템플릿

```markdown
# TODO 리스트 기능 추가

**Epic ID**: EPIC-m1n2o3p4
**Created**: 2025-11-11
**Status**: IN_PROGRESS
**Priority**: high

## Description
사용자가 할 일을 관리할 수 있는 기능을 추가합니다.

## Acceptance Criteria
- [ ] 할 일 추가 가능
- [ ] 할 일 완료 처리 가능
- [ ] 할 일 삭제 가능
- [ ] 필터링 (전체, 진행중, 완료)

## Tasks
- TASK-001: TodoList 컴포넌트 생성
- TASK-002: TodoItem 컴포넌트 생성
- TASK-003: useTodos 훅 생성
- TASK-004: 테스트 작성
- TASK-005: Storybook 스토리 작성

## Notes
- 로컬 스토리지에 저장
- 접근성 고려 (키보드 네비게이션)
```

#### TASK-001.md 템플릿

```markdown
# TodoList 컴포넌트 생성

**Task ID**: TASK-001
**Epic**: EPIC-m1n2o3p4
**Type**: feat
**Status**: TODO
**Assigned**: component-writer
**Created**: 2025-11-11 10:30:00

## Description
할 일 목록을 표시하는 TodoList 컴포넌트를 생성합니다.

## Acceptance Criteria
- [ ] TodoList.tsx 파일 생성
- [ ] Props 타입 정의
- [ ] JSDoc 주석 작성
- [ ] 접근성 속성 추가

## Implementation Notes
- TodoItem 컴포넌트를 사용하여 각 항목 렌더링
- 빈 목록일 때 "할 일이 없습니다" 메시지 표시

## Progress Log
- 2025-11-11 10:30:00: Task created
```

### 4. CLI 도구 (.devom/scripts/)

Epic과 Task를 관리하는 스크립트입니다.

#### epic-manager.sh

```bash
# Epic 생성
.devom/scripts/epic-manager.sh create "기능명" high

# Epic 목록
.devom/scripts/epic-manager.sh list

# Epic 상세
.devom/scripts/epic-manager.sh show EPIC-xxx

# Epic 완료
.devom/scripts/epic-manager.sh complete EPIC-xxx
```

#### task-manager.sh

```bash
# Task 생성
.devom/scripts/task-manager.sh create feat "작업명" component-writer

# Task 목록
.devom/scripts/task-manager.sh list

# Task 상태 업데이트
.devom/scripts/task-manager.sh update TASK-xxx IN_PROGRESS
```

---

## ⚙️ 에이전트 시스템 동작 원리

### 워크플로우 상세 분석

#### Step 1: 개발자의 요청

```
개발자: "사용자 프로필 페이지를 만들어줘.
        프로필 사진, 이름, 이메일, 소개를 보여줘야 해"
```

#### Step 2: Claude (Scrum Master)의 분석

```javascript
// Claude의 내부 프로세스 (개발자에게는 보이지 않음)

1. 요구사항 분석
   - 사용자 프로필 페이지 필요
   - 표시 항목: 프로필 사진, 이름, 이메일, 소개
   - 데이터 패칭 필요

2. Epic 생성 판단
   → Epic 필요 (여러 컴포넌트 + 훅 + 테스트)

3. Task 분해
   - TASK-001: UserProfile 컴포넌트 (메인)
   - TASK-002: UserAvatar 컴포넌트 (프로필 사진)
   - TASK-003: useUserData 훅 (데이터 패칭)
   - TASK-004: 테스트 작성
   - TASK-005: Storybook 스토리

4. 에이전트 선택
   - TASK-001, 002 → component-writer
   - TASK-003 → hook-writer
   - TASK-004 → component-writer (테스트)
   - TASK-005 → storybook-writer

5. 병렬 실행 계획
   - Phase 1: TASK-001, 002, 003 동시 실행
   - Phase 2: TASK-004, 005 동시 실행
   - Phase 3: code-reviewer로 전체 리뷰
```

#### Step 3: 에이전트 호출 (Self-Contained 프롬프트)

Claude가 에이전트에게 보내는 실제 프롬프트:

```typescript
// 🔵 Task 1: UserProfile 컴포넌트

파일 경로: /Users/dabom-choi/StudySource/devom/packages/ui/src/UserProfile/UserProfile.tsx

구현할 코드:
import { FC } from 'react';
import { UserAvatar } from '../UserAvatar';
import type { User } from '@devom/types';

/**
 * 사용자 프로필 표시 컴포넌트
 * @param user - 사용자 정보 객체
 * @returns 사용자 프로필 UI
 */
export interface UserProfileProps {
  user: User
}

export function UserProfile({ user }: UserProfileProps) {
  return (
    <div className="user-profile">
      <UserAvatar src={user.avatar} alt={user.name} />
      <h1>{user.name}</h1>
      <p className="email">{user.email}</p>
      <p className="bio">{user.bio}</p>
    </div>
  )
}

필요한 타입 정의:
interface User {
  id: string;
  name: string;
  email: string;
  avatar: string;
  bio: string;
}

의존성:
- React
- UserAvatar 컴포넌트
- @devom/types

코딩 규칙 (반드시 준수):
- 함수형 컴포넌트만 사용
- Props는 interface로 정의
- JSDoc 주석 필수 (@param, @returns)
- 접근성: 시맨틱 HTML 사용
- 최대 200줄
- camelCase 변수명
- PascalCase 컴포넌트명
```

#### Step 4: component-writer 에이전트 실행

```
component-writer 에이전트 내부 프로세스:

1. component-writer.md 읽기
   → 역할, 규칙, 제약사항 이해

2. 프롬프트 분석
   → 파일 경로 확인
   → 코드 확인
   → 타입 확인
   → 규칙 확인

3. coding-rules.md 참조
   → 코딩 표준 적용

4. 파일 작성
   → UserProfile.tsx 생성
   → 코드 적용

5. 타입 체크
   → npx tsc --noEmit 실행
   → 타입 에러 확인

6. 결과 보고
   ✅ UserProfile.tsx 생성 완료
   📦 Exports: UserProfile, UserProfileProps
   🎨 Features: 프로필 표시, 접근성 지원
```

#### Step 5: 병렬 실행 (동시에 여러 에이전트)

```
┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│ component-writer │  │ component-writer │  │  hook-writer     │
│                  │  │                  │  │                  │
│ UserProfile.tsx  │  │ UserAvatar.tsx   │  │ useUserData.ts   │
│                  │  │                  │  │                  │
│ 실행 중...        │  │ 실행 중...        │  │ 실행 중...        │
└──────────────────┘  └──────────────────┘  └──────────────────┘
         ↓                     ↓                     ↓
    15초 후 완료           12초 후 완료           18초 후 완료
         ↓                     ↓                     ↓
┌────────────────────────────────────────────────────────────┐
│          모든 파일 생성 완료 (18초 소요)                     │
│  순차 실행 시: 15 + 12 + 18 = 45초                         │
│  병렬 실행 시: max(15, 12, 18) = 18초 (2.5배 빠름)         │
└────────────────────────────────────────────────────────────┘
```

#### Step 6: code-reviewer 실행

모든 코드가 생성된 후 리뷰:

```
code-reviewer 에이전트 프롬프트:

다음 파일들을 리뷰해주세요:

파일 1: UserProfile.tsx
[전체 코드 포함]

파일 2: UserAvatar.tsx
[전체 코드 포함]

파일 3: useUserData.ts
[전체 코드 포함]

리뷰 기준:
1. 코딩 규칙 준수 (.devom/docs/coding-rules.md)
   - TypeScript 타입 안전성
   - 네이밍 컨벤션
   - 함수 길이 (최대 50줄)

2. 컴포넌트 패턴 (.devom/docs/component-patterns.md)
   - Props 인터페이스 설계
   - 컴포넌트 합성

3. 접근성 (a11y)
   - 시맨틱 HTML
   - ARIA 속성
   - 키보드 접근성

4. 성능
   - 불필요한 리렌더링
   - 메모이제이션

각 파일에 대해:
- ✅ PASS / ⚠️ WARNING / ❌ FAIL
- 구체적인 피드백 (라인 번호 포함)
- 수정 제안
```

#### Step 7: 최종 보고

```
Claude (Scrum Master):

✅ 사용자 프로필 페이지 구현 완료!

📦 생성된 파일:
1. packages/ui/src/UserProfile/UserProfile.tsx
   - Props: UserProfileProps
   - Exports: UserProfile

2. packages/ui/src/UserAvatar/UserAvatar.tsx
   - Props: UserAvatarProps
   - Exports: UserAvatar

3. packages/hooks/src/useUserData/useUserData.ts
   - Return: UseUserDataReturn (data, loading, error)
   - Exports: useUserData

4. packages/ui/src/UserProfile/UserProfile.test.tsx
   - 6 test cases
   - Coverage: 95%

📊 코드 리뷰 결과:
✅ PASS - 모든 파일이 코딩 규칙을 준수합니다
- TypeScript 타입 안전
- 접근성 지원 완료
- 성능 최적화 적용

📝 다음 단계:
1. yarn test          # 테스트 실행
2. npx tsc --noEmit  # 타입 체크
3. yarn storybook     # Storybook 확인

💡 사용 예시:
import { UserProfile } from '@devom/ui';
import { useUserData } from '@devom/hooks';

function ProfilePage() {
  const { data, loading } = useUserData('user-123');
  if (loading) return <div>Loading...</div>;
  return <UserProfile user={data} />;
}
```

---

## 🧩 Self-Contained 프롬프트 철학

### 핵심 원칙

**에이전트는 메모리 파일을 읽을 수 없습니다.**

따라서 모든 정보를 프롬프트에 포함해야 합니다.

### ❌ 잘못된 방법

```
개발자: "Button 컴포넌트 만들어줘"

Claude → component-writer:
"Button 컴포넌트를 만들어주세요."

component-writer:
"❌ 정보가 부족합니다. 다음이 필요합니다:
- 파일 경로
- 구현할 코드
- Props 정의
- 코딩 규칙"
```

### ✅ 올바른 방법

```
개발자: "Button 컴포넌트 만들어줘"

Claude → component-writer:
"파일 경로: packages/ui/src/Button/Button.tsx

구현할 코드:
import { FC, ButtonHTMLAttributes } from 'react';
import styles from './Button.module.css';

/**
 * 재사용 가능한 버튼 컴포넌트
 * @param variant - 버튼 스타일 (primary, secondary, danger)
 * @param size - 버튼 크기 (sm, md, lg)
 * @param loading - 로딩 상태
 * @param children - 버튼 내용
 */
export interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'secondary' | 'danger';
  size?: 'sm' | 'md' | 'lg';
  loading?: boolean;
}

export const Button: FC<ButtonProps> = ({
  variant = 'primary',
  size = 'md',
  loading = false,
  disabled,
  children,
  className,
  ...props
}) => {
  return (
    <button
      className={`${styles.button} ${styles[variant]} ${styles[size]} ${className || ''}`}
      disabled={disabled || loading}
      aria-busy={loading}
      aria-label={typeof children === 'string' ? children : undefined}
      {...props}
    >
      {loading ? 'Loading...' : children}
    </button>
  );
};


필요한 타입:
- ButtonHTMLAttributes (React)

코딩 규칙:
- 함수형 컴포넌트만 사용
- Props는 interface로 정의
- JSDoc 필수 (@param, @returns)
- 접근성: aria-busy, aria-label
- 기본값 설정 (variant, size)
- 최대 200줄"

component-writer:
"✅ Button.tsx 생성 완료!"
```

### Self-Contained 체크리스트

프롬프트에 반드시 포함해야 할 항목:

- [ ] **파일 경로** (절대 경로)
- [ ] **완전한 구현 코드** (imports 포함)
- [ ] **타입 정의** (interface, type)
- [ ] **의존성** (필요한 라이브러리, 컴포넌트)
- [ ] **코딩 규칙** (핵심 규칙 명시)
- [ ] **JSDoc** (함수/컴포넌트 설명)
- [ ] **예상 사용법** (선택, 도움이 됨)

---

## 🔄 실제 워크플로우

### 전체 프로세스

```
1. 개발자 요청
   ↓
2. Epic 생성 (.devom/scripts/epic-manager.sh)
   ↓
3. Git 브랜치 생성 (feature/EPIC-xxx)
   ↓
4. Claude가 Task 분해
   ↓
5. 각 Task별로 에이전트 호출 (병렬)
   ↓
6. 코드 생성 완료
   ↓
7. 테스트 실행 (yarn test)
   ↓
8. 타입 체크 (npx tsc --noEmit)
   ↓
9. 코드 리뷰 (code-reviewer)
   ↓
10. 수정 사항 반영 (있는 경우)
    ↓
11. 재검증
    ↓
12. Git 커밋
    ↓
13. Pull Request 생성
    ↓
14. Epic 완료
```

### 상세 단계별 가이드

#### 1단계: Epic 생성

```bash
cd /Users/dabom-choi/StudySource/devom

# Epic 생성
.devom/scripts/epic-manager.sh create "사용자 프로필 페이지 추가" high

# 출력:
# ✅ Epic created: EPIC-m1n2o3p4
# 📝 File: .devom/epics/EPIC-m1n2o3p4/EPIC.md
```

#### 2단계: Git 브랜치 생성

```bash
# 브랜치 생성
git checkout -b feature/EPIC-m1n2o3p4

# 확인
git branch
# * feature/EPIC-m1n2o3p4
#   main
```

#### 3단계: Claude에게 구현 요청

```
개발자: "EPIC-m1n2o3p4를 구현해줘"

Claude:
"네! Epic을 분석하고 Task로 분해하겠습니다.

📋 Epic: 사용자 프로필 페이지 추가

Task 분해:
- TASK-001: UserProfile 컴포넌트 생성
- TASK-002: UserAvatar 컴포넌트 생성
- TASK-003: useUserData 훅 생성
- TASK-004: 테스트 작성
- TASK-005: Storybook 스토리 작성

지금 시작할까요?"

개발자: "응!"

Claude: [에이전트들 호출 시작]
```

#### 4단계: 에이전트 병렬 실행

```
Phase 1: 구현 (병렬)
├─ component-writer → UserProfile.tsx
├─ component-writer → UserAvatar.tsx
└─ hook-writer → useUserData.ts

Phase 2: 테스트 & 문서 (병렬)
├─ component-writer → *.test.tsx
└─ storybook-writer → *.stories.tsx

Phase 3: 리뷰
└─ code-reviewer → 전체 파일 리뷰
```

#### 5단계: 검증

```bash
# 테스트 실행
yarn test
# ✅ All tests passed (6 tests)

# 타입 체크
npx tsc --noEmit
# ✅ No type errors

# Storybook 확인
yarn storybook
# → http://localhost:6006
```

#### 6단계: 커밋

```bash
git add .
git commit -m "feat: add user profile page

- UserProfile component
- UserAvatar component
- useUserData hook
- Tests (95% coverage)
- Storybook stories

Epic: EPIC-m1n2o3p4

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>"
```

#### 7단계: Pull Request

```bash
# PR 생성
gh pr create --title "feat: 사용자 프로필 페이지 추가" --body "$(cat <<'EOF'
## Summary
- UserProfile, UserAvatar 컴포넌트 추가
- useUserData 커스텀 훅 추가
- 테스트 커버리지 95%

## Epic
EPIC-m1n2o3p4

## Test Plan
- [x] 컴포넌트 렌더링 테스트
- [x] 훅 동작 테스트
- [x] 접근성 테스트
- [x] Storybook 확인

🤖 Generated with Claude Code
EOF
)"
```

#### 8단계: Epic 완료

```bash
# Epic 상태 업데이트
.devom/scripts/epic-manager.sh complete EPIC-m1n2o3p4

# 출력:
# 🎉 Epic completed: EPIC-m1n2o3p4
```

---

## 🚀 병렬 실행 최적화

### 병렬 실행의 장점

**순차 실행 (전통적 방식):**
```
컴포넌트 1 생성 (15초)
  → 컴포넌트 2 생성 (12초)
    → 훅 생성 (18초)
      → 테스트 작성 (20초)

총 시간: 15 + 12 + 18 + 20 = 65초
```

**병렬 실행 (DDK 방식):**
```
┌─ 컴포넌트 1 생성 (15초)
├─ 컴포넌트 2 생성 (12초)  } 동시 실행
└─ 훅 생성 (18초)

→ 테스트 작성 (20초)

총 시간: max(15, 12, 18) + 20 = 38초 (1.7배 빠름)
```

### 병렬 실행 전략

#### 독립적인 Task만 병렬 실행

```
✅ 병렬 가능:
- UserProfile 컴포넌트 + UserAvatar 컴포넌트
  (서로 독립적, 의존성 없음)

❌ 병렬 불가:
- UserProfile 컴포넌트 → UserProfile 테스트
  (테스트는 컴포넌트가 있어야 작성 가능)
```

#### Phase 단위 병렬 실행

```
Phase 1: 구현 (병렬)
  - 컴포넌트 A
  - 컴포넌트 B
  - 훅 C

Phase 2: 테스트 (병렬)
  - 컴포넌트 A 테스트
  - 컴포넌트 B 테스트
  - 훅 C 테스트

Phase 3: 리뷰 (단일)
  - 전체 파일 리뷰
```

### 최적 병렬 수

**권장: 3-5개 동시 실행**

```
✅ 3개 병렬:
- 안정적
- 에러 추적 용이
- 적당한 속도 향상

⚠️ 10개 병렬:
- 불안정할 수 있음
- 에러 발생 시 추적 어려움
- 과도한 리소스 사용
```

---

## 💡 실전 예제

### 예제 1: TODO 리스트 구현

#### 요청

```
개발자: "TODO 리스트를 만들어줘. 할 일 추가, 완료 처리, 삭제, 필터링이 가능해야 해"
```

#### Claude의 Epic 분해

```markdown
# Epic: TODO 리스트 기능

## Tasks

### Phase 1: 기본 구조 (병렬)
- TASK-001: TodoList 컴포넌트
- TASK-002: TodoItem 컴포넌트
- TASK-003: TodoInput 컴포넌트
- TASK-004: useTodos 훅

### Phase 2: 추가 기능 (병렬)
- TASK-005: TodoFilter 컴포넌트
- TASK-006: 로컬 스토리지 유틸리티

### Phase 3: 테스트 (병렬)
- TASK-007: 컴포넌트 테스트
- TASK-008: 훅 테스트

### Phase 4: 문서화 (병렬)
- TASK-009: Storybook 스토리
- TASK-010: README 작성

### Phase 5: 리뷰
- TASK-011: 전체 코드 리뷰
```

#### 에이전트 실행

```typescript
// Phase 1: 기본 구조 (4개 병렬)

// TASK-001: TodoList
<invoke name="Task">
<parameter name="subagent_type">code-writer</parameter>
<parameter name="prompt">
파일 경로: packages/ui/src/TodoList/TodoList.tsx

구현할 코드:
import { FC } from 'react';
import { TodoItem } from '../TodoItem';
import type { Todo } from '@devom/types';

export interface TodoListProps {
  todos: Todo[];
  onToggle: (id: string) => void;
  onDelete: (id: string) => void;
}

export function TodoList({ todos, onToggle, onDelete }: TodoListProps) {
  if (todos.length === 0) {
    return <p>할 일이 없습니다</p>
  }

  return (
    <ul role="list" aria-label="할 일 목록">
      {todos.map(todo => (
        <TodoItem
          key={todo.id}
          todo={todo}
          onToggle={onToggle}
          onDelete={onDelete}
        />
      ))}
    </ul>
  )
}

코딩 규칙:
- 함수형 컴포넌트
- Props 타입 정의
- JSDoc 필수
- 접근성 (role, aria-label)
</parameter>
</invoke>

// TASK-002, 003, 004도 동시에 실행...
```

#### 결과

```
✅ Phase 1 완료 (18초)
- TodoList.tsx
- TodoItem.tsx
- TodoInput.tsx
- useTodos.ts

✅ Phase 2 완료 (15초)
- TodoFilter.tsx
- localStorage.utils.ts

✅ Phase 3 완료 (25초)
- *.test.tsx (커버리지 92%)

✅ Phase 4 완료 (12초)
- *.stories.tsx
- README.md

✅ Phase 5 완료 (8초)
- 코드 리뷰 PASS

총 소요 시간: 78초
(순차 실행 시: ~150초)
```

### 예제 2: 사용자 인증 시스템

#### 요청

```
개발자: "로그인, 회원가입, 비밀번호 찾기 페이지를 만들어줘"
```

#### Epic 분해

```markdown
# Epic: 사용자 인증 시스템

## Tasks

### Phase 1: 공통 컴포넌트
- TASK-001: AuthLayout (레이아웃)
- TASK-002: FormInput (입력 필드)
- TASK-003: FormButton (버튼)
- TASK-004: ErrorMessage (에러 표시)

### Phase 2: 훅 & 유틸
- TASK-005: useAuth (인증 훅)
- TASK-006: useForm (폼 관리)
- TASK-007: validation.utils (검증)

### Phase 3: 페이지
- TASK-008: LoginPage
- TASK-009: SignupPage
- TASK-010: ForgotPasswordPage

### Phase 4: 테스트
- TASK-011: 컴포넌트 테스트
- TASK-012: 훅 테스트
- TASK-013: E2E 테스트 (Playwright)

### Phase 5: 문서화
- TASK-014: Storybook
- TASK-015: API 문서
```

---

## 📊 성능 지표

### 평균 작업 시간

| 작업 유형 | 순차 실행 | 병렬 실행 | 개선율 |
|----------|----------|----------|--------|
| 컴포넌트 3개 | 45초 | 18초 | 2.5배 |
| 컴포넌트 + 훅 5개 | 75초 | 30초 | 2.5배 |
| 전체 기능 (10개 파일) | 180초 | 65초 | 2.8배 |

### 코드 품질 지표

- **타입 안전성**: 100% (any 사용 없음)
- **테스트 커버리지**: 평균 85%
- **접근성 준수**: WCAG 2.1 AA
- **코드 리뷰 통과율**: 95%

---

## 🎓 Best Practices

### 1. Epic 크기 조절

```
✅ 적절한 Epic:
- 5-15개 Task
- 2-4시간 소요
- 명확한 목표

❌ 너무 큰 Epic:
- 30개 이상 Task
- 1주일 이상 소요
- 모호한 목표
→ 여러 Epic으로 분리
```

### 2. Task 의존성 관리

```
✅ 명확한 의존성:
Task A → Task B → Task C
(순차 실행 필요)

✅ 독립적인 Task:
Task A, B, C
(병렬 실행 가능)

❌ 순환 의존성:
Task A → Task B → Task A
(피해야 함)
```

### 3. 프롬프트 작성 팁

```
✅ 좋은 프롬프트:
- 완전한 코드 포함
- 모든 타입 정의
- 명확한 규칙
- 예상 사용법

❌ 나쁜 프롬프트:
- "버튼 만들어줘"
- "이거 수정해줘"
- "타입 에러 고쳐줘"
```

### 4. 에러 처리

```
에러 발생 시:

1. 에러 메시지 확인
2. 관련 파일 확인
3. 프롬프트 수정
4. 재실행

흔한 에러:
- 타입 에러 → 타입 정의 추가
- Import 에러 → 경로 확인
- 문법 에러 → 코드 검증
```

---

## 🔗 추가 리소스

### 문서

- [CLAUDE.md](../../../CLAUDE.md) - DDK 메인 가이드
- [coding-rules.md](./coding-rules.md) - 코딩 표준
- [component-patterns.md](./component-patterns.md) - 컴포넌트 패턴
- [testing-patterns.md](./testing-patterns.md) - 테스트 패턴

### 에이전트

- [component-writer.md](../../../.claude/agents/component-writer.md)
- [hook-writer.md](../../../.claude/agents/hook-writer.md)
- [storybook-writer.md](../../../.claude/agents/storybook-writer.md)
- [code-reviewer.md](../../../.claude/agents/code-reviewer.md)

### 도구

- `.devom/scripts/epic-manager.sh` - Epic 관리
- `.devom/scripts/task-manager.sh` - Task 관리

---

## ❓ FAQ

### Q1: 에이전트를 직접 호출할 수 있나요?

**A**: 아니요. 에이전트는 Claude (Scrum Master)만 호출할 수 있습니다. 개발자는 Claude에게 요청하고, Claude가 적절한 에이전트를 선택합니다.

### Q2: 여러 에이전트를 동시에 몇 개까지 실행할 수 있나요?

**A**: 권장 개수는 3-5개입니다. 더 많은 수도 가능하지만, 에러 추적이 어려워질 수 있습니다.

### Q3: Self-Contained 프롬프트가 왜 필요한가요?

**A**: 에이전트는 메모리 파일을 읽을 수 없기 때문입니다. 모든 정보를 프롬프트에 포함해야 에이전트가 독립적으로 작동할 수 있습니다.

### Q4: 코드 리뷰는 필수인가요?

**A**: 네, 모든 Phase마다 코드 리뷰가 필수입니다. 이를 통해 코드 품질을 보장합니다.

### Q5: Epic 없이 개발할 수 있나요?

**A**: 작은 수정(1-2개 파일)은 가능하지만, 새로운 기능은 Epic을 생성하는 것을 권장합니다.

---

## 📝 버전 히스토리

| 버전 | 날짜 | 변경 사항 |
|------|------|-----------|
| 1.1.0 | 2025-11-11 | DDK로 리브랜딩, 프론트엔드 전용 |
| 1.0.0 | 2025-11-11 | 초기 문서 작성 |

---

**Last Updated**: 2025-11-11
**Maintained by**: Claude Code + DDK
**Status**: Active (Frontend Edition)

