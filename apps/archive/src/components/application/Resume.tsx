import type { JSX } from 'react';

const Resume = (): JSX.Element => {
    return (
        <div className="h-full w-full">
            <h1>프론트엔드 개발자</h1>
        </div>
    );
};

export default Resume;
